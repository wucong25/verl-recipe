
# Note: this config is designed for quick CI functional tests. For more serious
# training, you MOST LIKELY will need to increase the amount of GPU resources and
# configure this file a bit more.

server:
  model_name: ${oc.env:TINKER_SERVER_MODEL_NAME,unsloth/gpt-oss-20b-BF16}
  host: ${oc.env:TINKER_SERVER_HOST,0.0.0.0}
  port: ${oc.env:TINKER_SERVER_PORT,8000}
  ray_address: ${oc.env:RAY_ADDRESS,local}
  checkpoint_dir: ${oc.env:TINKER_CHECKPOINT_DIR,/tmp/tinker-checkpoints}
  server_max_runtime: ${oc.env:TINKER_SERVER_MAX_RUNTIME,null} # an optional variable for the maximum uptime of the server, null means infinite
  max_concurrent_samples: ${oc.env:TINKER_SERVER_MAX_CONCURRENT_SAMPLES,32}
  enable_offload: true
  auto_merge_verl_default_config: true

actor_rollout_ref: # this is the same as verl's config, this field is ALWAYS required.
  # this is a bit more customized as it's a larger model
  actor:
    strategy: fsdp
    optim:
      lr_scheduler_type: constant
      lr: 1.0e-06
    fsdp_config:
      model_dtype: bfloat16
    ppo_mini_batch_size: 16
    ppo_micro_batch_size_per_gpu: 1
    # profiler:
    #   enable: true
    #   all_ranks: true
    #   tool_config:
    #     torch:
    #       contents: [cuda, cpu]

  rollout:
    name: vllm
    tensor_model_parallel_size: 8
    gpu_memory_utilization: 0.6
    quantization: null
    load_format: safetensors
    mode: async
    calculate_log_probs: true
    log_prob_micro_batch_size_per_gpu: 1
    enable_chunked_prefill: true
    max_num_batched_tokens: 8192
    free_cache_engine: true
    enforce_eager: false

  ref:
    enable: false

  model:
    path: ${oc.env:TINKER_SERVER_MODEL_PATH,unsloth/gpt-oss-20b-BF16}

trainer:
  logger:
  - console
  project_name: verl_tinker_server
  experiment_name: tinker_server_gpt_oss_20b_actor_rollout
  nnodes: 1
  n_gpus_per_node: 8

# global_profiler:
#   tool: torch
#   steps: [3]
#   profile_continuous_steps: false
#   save_path: outputs/profile
#   all_ranks: true
